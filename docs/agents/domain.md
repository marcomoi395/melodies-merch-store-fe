# Domain Docs

Before exploring, read root `CONTEXT.md`, relevant `docs/adr/`, or `CONTEXT-MAP.md` when present. Missing files: proceed silently.

This is a single-context repo: one root `CONTEXT.md`, root `docs/adr/`, and `src/`.

Use glossary vocabulary from `CONTEXT.md`. Surface conflicts with existing ADRs rather than overriding them.
